# CAF Actor Model Demo 配置文件
# 本文件演示 actor_system_config 如何加载配置文件中的参数
caf {
  scheduler {
    policy = "stealing"
    max-throughput = 500
  }
  logger {
    console {
      verbosity = "info"
      colored = true
    }
  }
}
